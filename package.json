{
  "name": "z-prefix---crud-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db": "knex migrate:rollback && knex migrate:latest && knex seed:run && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^10.7.0"
  }
}
